apiVersion: v1
kind: Service
metadata:
  name: messaging-service
  namespace: hiresphere
spec:
  type: LoadBalancer
  selector:
    app: messaging-service
  ports:
    - protocol: TCP
      port: 4006
      targetPort: 4006
