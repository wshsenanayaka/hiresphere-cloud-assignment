apiVersion: v1
kind: Service
metadata:
  name: messaging-service
  namespace: hiresphere
spec:
  type: ClusterIP
  selector:
    app: messaging-service
  ports:
    - port: 4006
      targetPort: 4006
