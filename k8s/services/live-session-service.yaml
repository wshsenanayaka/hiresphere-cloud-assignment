apiVersion: v1
kind: Service
metadata:
  name: live-session-service
  namespace: hiresphere
spec:
  type: LoadBalancer
  selector:
    app: live-session-service
  ports:
    - protocol: TCP
      port: 4007
      targetPort: 4007
