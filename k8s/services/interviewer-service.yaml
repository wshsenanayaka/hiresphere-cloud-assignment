apiVersion: v1
kind: Service
metadata:
  name: interviewer-service
  namespace: hiresphere
spec:
  type: ClusterIP
  selector:
    app: interviewer-service
  ports:
    - port: 4002
      targetPort: 4002
