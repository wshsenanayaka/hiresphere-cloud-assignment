apiVersion: v1
kind: Service
metadata:
  name: interviewer-service
  namespace: hiresphere
spec:
  type: LoadBalancer
  selector:
    app: interviewer-service
  ports:
    - protocol: TCP
      port: 4002
      targetPort: 4002
