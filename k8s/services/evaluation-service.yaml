apiVersion: v1
kind: Service
metadata:
  name: evaluation-service
  namespace: hiresphere
spec:
  type: LoadBalancer
  selector:
    app: evaluation-service
  ports:
    - protocol: TCP
      port: 4005
      targetPort: 4005
