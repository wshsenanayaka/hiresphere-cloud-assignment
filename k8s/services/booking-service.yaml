apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: hiresphere
spec:
  type: LoadBalancer
  selector:
    app: booking-service
  ports:
    - protocol: TCP
      port: 4003
      targetPort: 4003
