apiVersion: v1
kind: Service
metadata:
  name: package-service
  namespace: hiresphere
spec:
  type: LoadBalancer
  selector:
    app: package-service
  ports:
    - protocol: TCP
      port: 4008
      targetPort: 4008
