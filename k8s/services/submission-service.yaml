apiVersion: v1
kind: Service
metadata:
  name: submission-service
  namespace: hiresphere
spec:
  type: LoadBalancer
  selector:
    app: submission-service
  ports:
    - protocol: TCP
      port: 4004
      targetPort: 4004
